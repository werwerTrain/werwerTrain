apiVersion: v1
kind: Service
metadata:
  name: wwTrain-service
spec:
  selector:
    app: wwTrain
  ports:
    - protocol: TCP
      port: 9093
      targetPort: 9093
      nodePort: 30013
  type: NodePort
